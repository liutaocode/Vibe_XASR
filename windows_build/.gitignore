# Build output
bin/
obj/
dist/

# Installer artifacts (staged payload + built package — large, regenerated by build-installer.ps1)
installer/payload/
installer/*.msi
installer/*.wixpdb

# Models are fetched from HuggingFace, never committed
models/
*.onnx
tokens.txt

# IDE / OS cruft
.vs/
*.user
.DS_Store
