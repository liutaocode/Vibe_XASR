# macOS
.DS_Store

# SwiftPM build output
.build/

# .NET build output (windows_build)
windows_build/**/bin/
windows_build/**/obj/

# Release artifacts — rebuild via the per-platform build scripts.
# (built .app / notarized .dmg / Windows exe don't belong in git; ship via Releases)
macos_build/native/dist/
windows_build/dist/

# Debug symbols
*.dSYM/

# Re-downloadable sherpa archive (the extracted lib under native/sherpa/dist/ is kept)
*.tar.bz2

# ASR models downloaded on Windows (fetched from HuggingFace, not committed)
windows_build/**/models/

# Logs / scratch
*.log
/tmp/

# Claude Code 本地预览配置(静态服务器,各人本地用)
.claude/launch.json

# stray local archive (do NOT commit — 1.9GB)
/wind.zip

# Claude 设计工具导出的原始设计对话/上传件 — 不公开(docs 只保留 index.html + 架构图 + appcast.xml)
docs/chats/
docs/project/
docs/uploads/
docs/README.md
